{
  "name": "gpi-ecommerce",
  "private": true,
  "scripts": {
    "install:client": "npm install --prefix client",
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client",
    "vercel-build": "npm install --prefix client && npm run build --prefix client && node -e \"const fs=require('fs'); if(fs.existsSync('dist')) fs.rmSync('dist',{recursive:true,force:true}); fs.cpSync('client/dist','dist',{recursive:true});\""
  },
  "devDependencies": {}
}
